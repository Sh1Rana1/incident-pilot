# 事件时间契约

SLA 以两个事件实际发生时刻之间的 elapsed time 计算，而不是比较各自时区的墙上时间。
所有输入使用带 UTC offset 的 RFC 3339 时间戳；`Z` 表示 `+00:00`，其他 offset
也必须保留。计算前可以统一转换到 UTC，但不能直接丢弃时区信息。

边界包含在 SLA 内：elapsed time 等于配置的最大分钟数时仍算按时完成；完成时间
早于开始时间应视为无效区间。
